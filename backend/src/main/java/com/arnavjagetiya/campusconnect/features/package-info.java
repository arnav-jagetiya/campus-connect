/**
 * Root package for feature modules.
 * <p>
 * Each feature is organized into its own sub-package using plural naming
 * (e.g., users, clubs, events, posts, notifications, resources, admin).
 * Feature packages are created as each feature is implemented.
 */
package com.arnavjagetiya.campusconnect.features;
