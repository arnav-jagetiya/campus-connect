spring:
  # PostgreSQL — requires local database: CREATE DATABASE campus_connect;
  datasource:
    url: jdbc:postgresql://localhost:5432/campus_connect
    username: postgres
    password: Vikir@Van@IronBlood2006

  # JPA
  jpa:
    show-sql: true

# JWT — development secret only. Production must use APP_JWT_SECRET env var.
app:
  jwt:
    secret: Y2FtcHVzLWNvbm5lY3QtZGV2LXNlY3JldC1rZXktMjU2LWJpdHMtbG9uZw==

# Logging
logging:
  level:
    com.arnavjagetiya.campusconnect: DEBUG
